const extract = require('./extract')
const extractAndGenerate = require('./extract-and-generate.js')
const generate = require('./generate')
const validate = require('./validate')

module.exports = {
    extract,
    extractAndGenerate,
    generate,
    validate,
}
